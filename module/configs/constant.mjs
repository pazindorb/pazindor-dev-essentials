export function prepareConstants() {
  PDE.CONST = {
    SOCKET: {
      EMIT: {
        INPUT_DIALOG: "INPUT_DIALOG",
      },
      RESPONSE: {
        INPUT_DIALOG: "INPUT_DIALOG_RESPONSE"
      }
    }
  }
}